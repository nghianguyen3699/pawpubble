<span><%= link "Back", to: Routes.page_path(@conn, :index) %></span>
<h1>List Products</h1>
<table>
    <thead>
        <th>Id</th>
        <th>Name</th>
        <th></th>
    </thead>
    <tbody>
        <%= for shipping <- @shippings do %>
            <tr>
                <td><%= shipping.name %></td>
                <td>
                    <span><%= link "Delete", to: Routes.shipping_path(@conn, :delete, shipping), method: :delete, data: [confirm: "Are you sure"]%></span>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>
<span><%= link "New", to: Routes.shipping_path(@conn, :new) %></span>
